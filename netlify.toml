[build]
  publish = "."
  command = ""

[build.environment]
  # Firebase web apiKey is public-by-design (security enforced via
  # Firebase Security Rules, not secrecy). Disable Netlify's secrets
  # scanner so it doesn't block deploys on the embedded Firebase config.
  SECRETS_SCAN_ENABLED = "false"
